# 2026-08-31 14h17 · Direct Face Extrude Normal Vector Gizmo Overlay

## Where this repo actually was

`main` branch with 34 unit and integration test suites passing.

## Walkthrough · as built

- **Direct Face Normal Vector Gizmo Overlay**:
  - Enhanced the Sub-object Inspector Overlay in `app/src/shell.rs` to render direct normal vector push-pull extrude quick action controls (`−10 mm`, `−5 mm`, `+5 mm`, `+10 mm`, `+25 mm`).
  - Allows quick, precise face extrusion along normal vectors right on top of selected face sub-objects.

## Verified, and not

**Verified.**
- `make test`: All 34 unit and integration test suites pass 100%.
- `make check`: All workspace crates compile cleanly.
- `make clippy`: `-D warnings` clean across workspace crates.
- `cargo fmt --all --check`: Clean formatting across the repository.

## Walkthrough · as completed

Direct Face Normal Vector Gizmo Overlay implemented and verified.
