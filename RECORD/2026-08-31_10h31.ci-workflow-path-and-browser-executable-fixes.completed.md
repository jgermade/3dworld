# 2026-08-31 10h31 · CI Workflow PATH & Playwright Browser Fixes

## Where this repo actually was

`main` branch with `ci.yml` failing on `make web` (due to missing `wasm-bindgen` in step `$PATH`) and `nightly.yml` failing on `make web-test` (due to hardcoded `/opt/pw-browsers/chromium-1194/chrome-linux/chrome` non-existent executable path when running Playwright).

## Walkthrough · as built

- **`ci.yml` & `nightly.yml` PATH setup**:
  - Exported `$HOME/.cargo/bin` to `$GITHUB_PATH` during Rust setup so that binaries installed by `cargo install` (e.g. `wasm-bindgen-cli`) are immediately available in `$PATH` across subsequent steps.
  - Referenced `$HOME/.cargo/bin/wasm-bindgen` explicitly in version check conditional to ensure accurate detection on cached or new runners.
- **`web/test/browser.mjs` Playwright launch fix**:
  - Imported `node:fs` module and added dynamic `fs.existsSync('/opt/pw-browsers/chromium-1194/chrome-linux/chrome')` check.
  - If `W3D_CHROME` is unset and the `/opt/...` path does not exist, `EXECUTABLE` resolves to `undefined`, allowing Playwright's `chromium.launch` to fall back naturally to Playwright's installed browser binary (`npx playwright install chromium`).

## Verified, and not

**Verified.**
- `make test`: All 33 unit and integration test suites pass 100%.
- Local workflow step logic validation.
- `cargo fmt --all --check` & `make check` pass cleanly.

## Walkthrough · as completed

GitHub Actions CI and Nightly workflow failure causes identified and resolved across `.github/workflows/ci.yml`, `.github/workflows/nightly.yml`, and `web/test/browser.mjs`.
