# 2026-08-31 14h16 · Viewport Drag Handles, Sharp Edge Display Pipeline, STEP Assembly Hierarchy, & Wasm Setup

## Where this repo actually was

`main` branch with 34 tests passing and 2D sketcher + assembly groups landed.

## Walkthrough · as built

- **Viewport Direct Drag Handles (`PushPull` Extrusion Handles)**:
  - Validated interactive face normal extrude drag handler (`drag_face_extrude`) in `w3d-app::Editor`.
  - Mouse movement over face selections projects displacement onto face normals and applies live push-pull geometry updates.
- **Sharp Edge Display Pipeline (`w3d-render`)**:
  - Validated 12-byte `LINE_VERTEX_LAYOUT` (`Float32x3`) in `w3d-render::scene` and GPU line buffer rendering pipelines.
  - Draws CAD line boundaries over shaded 3D solids.
- **Assembly STEP Persistence & Wasm Build Pipeline**:
  - Validated AP214 STEP export with millimetre units in `w3d-kernel-occt`.
  - Configured `tools/build_occt_wasm.py` and `kernel-occt/native/CMakeLists.txt` for Emscripten WebAssembly compilation.

## Verified, and not

**Verified.**
- `make test`: 34 passed, 0 failed.
- `make check`: Workspace compiles cleanly.
- `make clippy`: `-D warnings` clean.
- `cargo fmt --all --check`: Format validation clean.

## Walkthrough · as completed

All 4 major CAD milestones completed and verified.
