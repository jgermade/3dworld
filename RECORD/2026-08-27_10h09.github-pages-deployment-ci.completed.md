# 2026-08-27 10h09 · Add GitHub Pages deployment to CI workflow

## Where this repo actually was

`w3d-web` (`web/`) already builds a WebAssembly application for `wasm32-unknown-unknown` using `wasm-bindgen` and `TruckKernel` (`w3d-kernel-truck`). `.github/workflows/ci.yml` previously tested `make wasm` to ensure compilation, but did not build or deploy the static web application (`index.html`, `loader.js`, and `web/dist/`) to GitHub Pages.

## Walkthrough · as built

1. **`.github/workflows/ci.yml`**: Added a `pages` job that runs on `ubuntu-latest` when the `test` job succeeds on `main`, `feat/antigravity`, or via `workflow_dispatch`. It compiles `make web` using `wasm-bindgen-cli` matching `Cargo.lock` and deploys the generated static app via official `actions/upload-pages-artifact@v3` and `actions/deploy-pages@v4`.

## Bugs found by building, not by reading

- None.

## Verified, and not

**Verified.**
- `make wasm`: compiles Wasm target cleanly.
- `make test`: all checks, lints, licence checks, and unit tests pass cleanly.

**Not verified.**
- Execution of the Pages job on GitHub Actions servers upon push.

## Loose ends, deliberately left

- None.

## Next

- None pending in this record file.

## Walkthrough · as completed

Added GitHub Pages continuous deployment job `pages` to `.github/workflows/ci.yml` using `actions/upload-pages-artifact@v3` and `actions/deploy-pages@v4` after building WebAssembly assets with `make web`.

### Correction · 2026-08-27 · Correct action versions in workflow files

Workflow files `.github/workflows/ci.yml` and `.github/workflows/nightly.yml` contained invalid non-existent action version tags (`actions/checkout@v7`, `actions/cache@v6`, `actions/upload-artifact@v7`, `actions/download-artifact@v7`) which caused GitHub Actions workflow validation failures. Updated all of these references to the valid current major release versions (`actions/checkout@v4`, `actions/cache@v4`, `actions/upload-artifact@v4`, `actions/download-artifact@v4`).

## Extension · 2026-08-27 · CI build dependency and cross-platform notice check fixes

1. **Linux Build Dependencies**: Added `build-essential pkg-config libgtk-3-dev libxkbcommon-dev libwayland-dev libx11-dev libocct-ocaf-dev` to `apt-get` steps in `.github/workflows/ci.yml` so GTK3, X11/Wayland, and OpenCASCADE OCAF/XCAF headers (`TDocStd_Document.hxx`, `XCAFDoc_DocumentTool.hxx`) build cleanly in CI.
2. **OpenCASCADE Auto-Detection**: Enhanced `kernel-occt/build.rs` and `ci.yml` to auto-detect versioned include paths (e.g. `/usr/include/opencascade*`) and multiarch library directories (`/usr/lib/x86_64-linux-gnu`).
3. **Cross-Platform NOTICE Determinism**: Updated `tools/notice.py` to run `cargo fetch` before scanning and normalize CRLF line endings to LF, ensuring `python3 tools/notice.py --check` generates identical notice content on macOS, Linux, and GitHub Actions CI.

## Extension · 2026-08-27 · Resolving CI step failures and test concurrency

1. **Notice Crate Version Keying**: Updated `tools/notice.py` to key third-party packages by `(name, version)` tuple rather than `name` alone, retaining all package versions across target filters deterministically and preventing sorting errors or target-order discrepancies.
2. **Clippy & Formatting Compliance**: Fixed `unnecessary_map_or` lint in `kernel-occt/build.rs` using `is_some_and` and formatted `build.rs` with `cargo fmt`, ensuring `make test` and `make test-occt` clippy steps pass without `-D warnings` errors.
3. **OpenCASCADE Runtime Dynamic Linking**: Added `cargo:rustc-link-arg=-Wl,-rpath,{lib_dir}` to `kernel-occt/build.rs` so dynamic libraries (`libTKernel.so.7`, etc.) are located at runtime on Linux.
4. **Test Concurrency**: Added `-- --test-threads=1` to `make test-occt` in `Makefile` to prevent data races in OpenCASCADE's C++ static global state (`Interface_Static`, `Message::DefaultMessenger`).
5. **Ubuntu 24.04 Pip Compatibility**: Updated `ci.yml` to pass `--break-system-packages` to `pip install steputils` on Ubuntu 24.04 runners.


