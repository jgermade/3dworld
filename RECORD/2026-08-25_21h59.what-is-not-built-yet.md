# 2026-08-25 21h59 · What is not built yet

**This file is the plan.** It is the only open file in `RECORD/`, and it supersedes the register
in `2026-08-25_19h22.what-is-not-built-yet.completed.md`, which took over from four `Next` blocks
before it.

It records no work. It exists so the plan is something a reader can read rather than reconstruct,
and it has no `Walkthrough · as built` and will never grow one — work that acts on it opens its
own file and says which items it took.

**It is also the one file in `RECORD/` that is maintained rather than appended.** The
never-rewrite rule protects a *record*, where the wrong turns are most of the value; this is a
*plan*, and a plan that only grows stops being one. Items leave it as they are done and `## Taken`
at the end says which file took each, so the reasoning stays one link away. Every other file in
the folder is append-only.

## Where this repo actually was

Eight crates. 65 native tests, 17 more against OpenCASCADE, 17 checks in a real browser, and a
window that opens.

```
kernel/        w3d-kernel       the seam: 16 methods + the conformance suite    ✅
kernel-fake/   w3d-kernel-fake  the contract without geometry                   ✅
core/          w3d-core         document · history · selection · mesh cache     ✅
format/        w3d-format       the .w3d file, specified in FORMAT.md           ✅
kernel-occt/   w3d-kernel-occt  OpenCASCADE through a 16-entry C ABI            ✅ native
                                                                                ⬜ wasm
render/        w3d-render       wgpu · camera · ID-buffer picking               ✅
web/           w3d-web          loader · probe · COOP/COEP · a canvas           ✅ WebGL2
                                                                                ✅ threaded
app/           w3d-app          editor · scene · winit and egui in one pass     ✅ desktop
kernel-truck/  w3d-kernel-truck pure Rust: exact surfaces, real edges, and a
                                boolean that is narrow rather than absent      ◐
kernel-native/                  ours, or truck                                  ⬜
```

A person can open a window, add primitives, select them, run a boolean on real OpenCASCADE
geometry, orbit, pan, undo, save and reopen — and export what they made as STEP, or import one,
including files written by Pro/ENGINEER and Siemens NX. **No CAD program has yet opened a file this
one writes**: a parser with no OCCT in it has read one and found the solids in it, which is
evidence about the file and not about the geometry.

The browser runs the same viewport on `TruckKernel`, in either a single-threaded or a threaded
build, and the threaded one meshes a solid's faces across a rayon pool. **Since 2026-09-05 it
models**: the plate on the page has a hole in it because a boolean cut one, and the page says so.
What that boolean will do is narrow — see the small items — and there is still no OCCT in wasm.

## Verified, and not

Gathered from the closed files so a reader does not have to open five of them. This is the half
that decides what is worth doing next.

**Verified.**

| | |
| --- | --- |
| `make test` | 65 tests, no setup. Arena, document, history, the zip, the format round-trip, the editor's rules, the camera, mesh packing, conformance against the fake — including that a backend which does not do STEP refuses in both directions and says so in the status line. |
| `make test-occt` | 17 against OpenCASCADE 7.6.3: the conformance suite, five document tests, the drilled plate through the GPU, a document saved to BREP and reopened, and six on STEP — the bytes asserted to be a part-21 file by inspection, and read back by a kernel that never saw the geometry. It also lints the crate, which nothing did before. |
| `make app-test-step` | A STEP file written by one process and drawn by another, with OCCT and a display. The only place the STEP path runs as the *program* rather than as a test harness. |
| `make step-check` | STEP against something that is not us. A file this program wrote, read by a pure-Python part-21 parser with no OCCT in it: every reference resolved, both solids reachable from a shape representation, faces counted by surface type, millimetres. Three files from Pro/ENGINEER, Siemens NX and ST-Developer through the real reader — two imported, measured and meshed, one **refused** because it is a surface model. Inputs pinned by SHA-256, committed nowhere. |
| `make licences` | 328 third-party crates across three builds, every one compatible with GPL-3.0-or-later, with fourteen negative controls that run first. |
| `make wasm` | The wasm-bound crates build for `wasm32-unknown-unknown`, **and** the WebGL2 backend is really in the tree. |
| `make web-test` | Checks in Chromium, in four runs: WebGPU offered, WebGL2 forced, no COOP/COEP and no service worker (which must degrade visibly), and no COOP/COEP *with* the service worker, which must end up cross-origin isolated and threaded. **32 of 32 pass** in the configuration without the threaded bundle built, which is the run `2026-09-05_13h14` made; the two that had failed since before the threaded work now report **92 distinct colours where they reported 5**, because the plate has a hole in it. One of the 32 is new and asserts exactly that: the boolean the page asks for succeeded. |
| `make app-test` | A real X11 window under Xvfb, thirty frames, chrome and scene asserted to have drawn *separately*. |
| clippy, fmt | `-D warnings`, no allow-list. Clean on Rust 1.98. |

**Not verified, and the risk taken with each.**

- **WebGPU has never rendered a pixel, anywhere.** Native runs are Vulkan through lavapipe, which
  is not WebGPU; the browser's WebGPU reports an adapter and draws nothing. The largest single
  unknown, and the only thing established about it is that a browser can claim it falsely.
- **Nothing about how it *feels*.** Thirty frames under a software rasteriser prove the pipeline
  runs. Latency while dragging is the only thing that matters in a modeller and cannot be measured
  here.
- **One browser, one windowing system.** Chromium and X11 under Xvfb. No Firefox, no Safari, no
  Wayland, no macOS, no HiDPI.
- **A skipped test looks like a passed test.** `w3d-render`'s tests print `SKIPPED:` without an
  adapter and `cargo test` still says `ok`; `make web-test` and `make app-test` are not part of
  `make test` at all.
- **Little geometry is verified beyond the contract.** Since 2026-09-05 the conformance suite has a
  geometry half — the volume a mesh encloses, and what a boolean does to it. Later the same day it
  also gained a **golden census** that compares a *boundary*: the material in each plane, where its
  centre of area is, the total area lying in no named plane, and the rule that a face id names one
  connected region. That closed the "weighs rather than compares" half of this item, and the
  centroid is what makes it worth having — a hole drilled in the wrong place weighs exactly what a
  hole drilled in the right place weighs.
  What is left is the fixtures. There are **two**: a box and a drilled plate. Both are axis-aligned,
  neither is degenerate, and nothing among them is a union, a fillet, a sweep or a loft. The suite
  gained the ability to compare a boundary and has barely used it. **`OcctKernel` has never run any
  of it** — OCCT was not installed where it was written — so what holds the census against the
  second backend that does geometry is CI and nothing else.
- **The suite encodes what three backends agree on**, one of which does no geometry and says so.
- **No CAD program has read a STEP file this one writes.** A parser that shares no code with OCCT
  now reads one and is held to resolving every reference, finding the solids in the model and
  counting the faces by surface type — but it reads the *file*, not the *model*: nothing in it
  builds a solid or would notice one that is not closed. Fusion, FreeCAD, SolidWorks and Onshape —
  the whole reason STEP was chosen — have still never seen one, and nothing that could is
  installable in the environment these sessions run in.
- **Three foreign files is three.** AP203 and AP214 import; no AP242, nothing in inches, nothing
  from CATIA or SolidWorks, nothing pathological. **The unit conversion has still never run**,
  because all three samples are in millimetres.
- **An import is not compared to anything.** Bodies, faces and triangles are printed, not asserted
  against a golden number: a wrong-but-well-formed import passes.
- **No implementation has been written from `FORMAT.md`**, which is the only test of whether that
  document is sufficient. One writer and one reader from the same head agreeing proves less than
  it looks like. There are also no old files, so forward compatibility is asserted by a test that
  edits a version number rather than by history.
- **Every performance number in this repository is somebody else's.** Nothing has been measured on
  this workload, because there is not yet a workload. The threaded browser build reports a
  tessellation time on the page, and it is 0–2 ms on a scene of twelve triangles: a place to put a
  number, not a number. **Nothing here is evidence that threads made anything faster.**
- **The suite's floor under "a backend may decline" is three box fixtures.** Since 2026-09-05 a
  backend may refuse an operation and still conform, so that a narrow real boolean is not treated
  as worse than a fake one; the union, difference and intersection of two boxes are mandatory so
  that refusing everything cannot pass. That is a floor, not a ceiling: a backend that does those
  three and declines the rest is conforming, and is not a modeller.
- **`f32` mesh positions are absolute.** A document kilometres from the origin has lost precision
  before the camera sees it, and the camera's `f64` cannot recover it.
- **The licence texts are collected but nothing assembles a distribution.** `make notice` writes
  `NOTICE` — 328 crates, and the fonts egui embeds, whose licences oblige their notices to be
  preserved — and `make notice-check` fails `make test` when it is stale. What does not exist is
  anything that *ships* it beside a binary or a `.wasm`.

## The pending register

Ordered by what blocks what, not by size.

**The nine items that contradicted `## Taken` were adjudicated on 2026-09-05**, by reading the code
for each rather than the list, in
[`2026-09-05_14h34.a-census-that-can-say-where.completed.md`](./2026-09-05_14h34.a-census-that-can-say-where.completed.md).
Six were genuinely done and are gone from here: edges on `Mesh`, truck as a second backend,
`BRepBuilderAPI_GTransform`, fillets, user-grouped transactions, and arena compaction. Three were
**half** done, and in each the half still owed is not the half the item described — so they are
rewritten below rather than removed, and each now says what landed and what did not. The
renumbering that follows is the first this register has had. The old numbers are quoted in the
rewritten items, and **every reference to an item number in `## Taken` below, and in any closed
file, is to the numbering before 2026-09-05** — those sentences are about the register as it stood
and are left standing.

### Blocking a modeller somebody can actually use

1. **A CAD program opening a file this one writes.** The half of the old item 1 that no check
   here can close, and it is now the *only* claim about interchange resting on nothing outside
   this repository. `make step-check` has an independent parser reading the file and finding the
   solids in it; what nobody has is a second *geometry* kernel building them. It needs an
   environment with FreeCAD in it — this one's apt sources have none — or a person, once, with
   Fusion or Onshape open and this file in front of them.
   The other half is done: files from Pro/ENGINEER, Siemens NX and ST-Developer import, and a
   surface model from the wild is refused by name. What is still missing there is **a sample in
   inches** — so the unit conversion has never run — **and one in AP242**, and a golden number to
   compare an import against rather than counting that solids came out.
2. **The seam does not carry an assembly hierarchy that both ends already have.** *Was item 2,
   "Assembly structure, product names, and instancing"; rewritten 2026-09-05 after the code was
   read.* Two thirds of that item landed and the register never noticed. **Product names land**:
   `w3d_occt.cpp` runs `STEPCAFControl_Reader` with XDE, reads each label's `TDataStd_Name`, and
   `ImportedBody::name` carries it up. **The document gained a hierarchy** it did not have when the
   item was written — `Node::parent`, `Node::children`, `Document::add_group`, `reparent`,
   `children_of`.
   What did not land is the wire between them. `extract_label` walks the assembly tree recursively
   and **flattens it**, pushing every solid it meets into one flat `std::vector` and throwing the
   structure away; `ImportedBody` has a name and no parent. So the number that made this item is
   unchanged: the AS1 assembly from Pro/ENGINEER holds **five** distinct solids and still imports
   as **eighteen** sibling bodies, five parts placed eighteen times with nothing able to say so.
   The remaining work is therefore much narrower than it was — a parent on `ImportedBody`, the
   indices to fill it in `w3d_occt.cpp`, and `Document::import` building the tree it can already
   hold. Colours and materials arrive by the same road.
3. **WebGPU on hardware that works.** See `Verified, and not`. Until then the fast path is an
   argument, and so is every performance claim resting on compute shaders.

### Blocking the wasm target

4. **An OCCT build for Emscripten.** `kernel-occt` is excluded from `make wasm`, so the browser
   has no exact kernel in it. It is no longer a `FakeKernel` bounding box — `TruckKernel` has
   tessellated the browser's scene since 2026-08-27 — but the boolean's narrowness is what that is worth: real
   surfaces, and no boolean under them. The toolkit list in `build.rs`
   is the input. Two things must be *decided* rather than discovered: `-fwasm-exceptions` against
   `-fexceptions`, which has real size and speed consequences; and a real upstream pin —
   `native/UPSTREAM` names a version, it does not enforce one. **STEP made this bigger**: five more
   toolkits are in the link now — `TKXSBase`, `TKSTEPBase`, `TKSTEPAttr`, `TKSTEP209`, `TKSTEP` —
   and the last is wanted by nothing here and will not link without the one before it.
5. **The tessellation format across the worker boundary.** Hand-waved as "transferable
   `ArrayBuffer`" in the first session and never designed. It has a fixed point at the GPU end now
   — 28-byte interleaved vertices, de-indexed when face ids demand it — so there is something to
   design against.

### Owed, but not blocking

6. **A GPU in CI.** The browser half is done and so is CI itself — `make test`, OCCT, step-check,
   the window under Xvfb and FreeCAD on every push, the browser nightly — so a green run is now
   evidence of something. It is still not evidence about a **driver**: the window job draws on
   lavapipe and the browser job on headless Chromium's software path, and WebGPU has never
   rendered a pixel anywhere. A GitHub runner has no GPU, so this needs a self-hosted runner with
   one, or a person, a machine and a screenshot.
   Both jobs now *say* so rather than leaving it to be inferred: since
   `2026-09-03_01h25.what-the-adapter-will-admit.completed.md` the report names what is behind the
   adapter, and every run of it anywhere has said `software (CPU rasteriser)`. The `Hardware`
   branch has never been taken.
7. **Nothing ships the NOTICE beside the thing it is a notice for.** *Was item 8, "A NOTICE file
   for a distribution"; rewritten 2026-09-05 after the code was read.* The file exists and is kept
   honest: `tools/notice.py` collects the licence text of every crate and of the fonts egui embeds,
   `make notice` writes it, and `make notice-check` fails `make test` when it is stale.
   What has never existed is the last step, which is the one the licences actually require.
   `make web` builds `web/dist/` and **does not copy `NOTICE` into it**; nothing puts it beside a
   desktop binary either. The fonts are OFL-1.1 and Ubuntu-font-1.0 and both oblige their notices
   to be *preserved*; MIT and Apache-2.0 want the same, and GPL-3 §6 wants the corresponding
   source. Serving a `.wasm` is distribution, and today it is distribution with the notice left on
   the build machine.
8. **`wasm-opt`, brotli, and a real payload number.** `make web` reports **4.25 MiB** of wasm and
   **1.28 MiB** gzipped, up from 3.34 and 1.12 when `truck-shapeops`, `truck-meshalgo` and `spade`
   joined the tree on 2026-09-05. Both are upper bounds and lazy ones, and OCCT will dwarf them.

9. **Degenerate input is only tested on the backend the browser does not use.** *Was item 10,
   "Fixture regression with degenerate input"; rewritten 2026-09-05 after the code was read.*
   `kernel-occt/tests/degenerate.rs` has eight fixtures — coincident faces in both directions, a
   cylinder tangent to a box face, spheres touching at a point, a thin wall, a micro-radius fillet,
   and degenerate sweep and loft inputs. That is real coverage and it was not there when this item
   was written.
   It is also **entirely inside the OCCT crate**, so it runs only under `make test-occt` and only
   against OpenCASCADE. `TruckKernel` — the backend that is actually in the browser, whose boolean
   is narrow, and which panics rather than failing on geometry it cannot handle — has no degenerate
   fixture anywhere, and neither does `kernel/src/conformance.rs`, so nothing in `make test` covers
   any of this. Since 2026-09-05 there is a census in the suite that could hold a degenerate result
   to a boundary rather than a bounding box; the fixtures to run through it do not exist.
10. **Measure something.** The 4 GB sharding argument is still an argument. A tessellated body of
    known size through the worker boundary turns it into a number — and there is now a candidate
    workload that costs nothing to obtain: the 15 MB assembly beside the samples
    `make step-samples` already fetches. It is also the first honest number about a synchronous
    import stalling the frame.
11. **The project's name.** `3dworld` is a directory. Crates are `w3d-` because `3dworld` is not a
    valid Rust identifier. Costs nothing now, costs a rename later.

### Small, and easy to lose

- **`truck`'s boolean is narrow, and the shape of the narrowness is measured rather than
  understood.** It does box against box in all three operations, and drills a plate with a
  cylinder. It does **not** union two cylinders at any tolerance from 0.05 to 0.001 — `None` or a
  panic — and `cylinder - box` and `cylinder | box` return `None` where `box - cylinder` succeeds,
  so the operation is not symmetric in its operands. A user meets this in the second thing they
  try. From `2026-09-05_13h14.a-boolean-and-a-suite-that-can-tell.completed.md`.
- **A panic inside `truck-shapeops` is fatal in the browser.** It panics rather than failing on
  geometry it cannot handle. `catch_unwind` turns that into an error on the desktop and does
  nothing on `wasm32-unknown-unknown`, which aborts; the backend's `singular` set keeps the one
  case known to abort — a sphere — from reaching it. Neither covers a case nobody has found yet.
  The real answers are a wasm build with unwinding, a pre-flight check cheaper than the operation,
  or OCCT in wasm, which makes it moot.
- **A sphere read from a file is not known to carry a pole.** `singular` records what the backend
  *built*, so a saved sphere, reopened and used as a cutting tool, reaches `truck-shapeops` on the
  web with nothing in front of it.
- **A `truck` boolean does not run at the document's tolerance.** It runs at
  `max(tol.linear, largest_extent * 1e-3)`, because `truck-shapeops` uses that number as the sag
  it divides an intersection curve into a polyline at. So a drilled hole is a polygon with a few
  hundred sides and its volume is 0.006% out. Exercised at 4 mm and 40 mm; nothing has been cut at
  0.4 mm or at 4 m.
- **`TruckKernel::bounds` are the triangulation's, not the surfaces'.** A curved face may bulge
  past them by up to `BOUNDS_SAG` (0.005). It replaced an answer that gridded the untrimmed
  surface and reported points the body does not contain, so this is a bound that can be honoured
  rather than a tighter one.
- **`truck` meshes are about 2.5x denser than they were**, because a trimmed face is a constrained
  Delaunay triangulation rather than four corners of a grid. Nothing has measured what that costs
  the browser beyond the 424 ms the page reports for its own scene on one thread.
- **`vtkio` brings `nom 3.2.1` and `quick-xml 0.22.0` into the host build**, both carrying
  future-incompatibility warnings, for a VTK feature nothing here calls. It cannot be switched off
  from this workspace: `truck-shapeops` depends on `truck-meshalgo` with default features.
- **The truck conformance and tessellation tests take about 18 seconds in debug**, up from under
  one, because they now do real booleans — and `make check` runs the tessellation file twice, once
  per setting of `parallel`.
- **`Live::resize` is wired and untested.** It reconfigures the surface, the depth texture and the
  capture target, and nothing exercises any of it. The most likely thing in the shell to be wrong.
- **The pick path is asserted at both ends and not in the middle**, where the shell holds a
  `PickPending` across frames.
- **Saving and exporting are synchronous and on the frame.** A large document will stall the
  window, and a STEP export stalls it for longer than a save: it rebuilds the model into another
  representation rather than writing bytes it already has.
- **`Save` with no path writes `untitled.w3d` in the working directory**, because there is no file
  dialogue and adding one is a dependency. The same gap is why there is no *Import STEP* button —
  export can invent a name beside the document and import needs one that exists — and why
  `--import-step` is a command-line option. Everything about files in this program is waiting on
  this one dependency.
- **An import only reframes the camera when the document was empty.** Importing into a populated
  document can leave the new bodies off screen, which looks exactly like an import that did
  nothing.
- **An exported STEP file is not byte-deterministic.** `FILE_NAME` carries a timestamp, so two
  exports of the same body differ. Worth knowing before somebody writes a golden-file test.
- **Face ids do not survive a STEP round-trip.** Faces come back renumbered, so a document that
  exports and re-imports loses every per-face selection. That is STEP rather than a defect, and it
  is the second place face identity has come up — the first was the old item 3, edges on `Mesh`,
  which landed. Since 2026-09-05 the conformance suite asserts the property those two share: a face
  id names **one connected region** of the surface, so two faces can no longer wear one name.
- **No units in a document.** `tolerance` implies a scale and nothing states one. A version-2
  conversation for `FORMAT.md`.
- **No thumbnail, no camera, no selection in a file.** The first every file browser wants; the
  other two are what "reopen where I left off" needs.
- **Zip64 is absent rather than handled.** A document over 4 GiB fails to save, and nothing tests
  that path.
- **`fake-csg-1`'s decoder is recursive over untrusted input.** It is the test kernel and
  `FORMAT.md` says not to implement it, but the bytes are still parsed by a real program.
- **Nodes have no stable id across a save.** Names are the only identity in a file, so nothing
  that wants to refer to a body between sessions has anything to hold.
- **A surface-only STEP file is refused**, and the first foreign file tried was one. Correct — a
  `Body` is a solid and the contract says so — but a modeller that cannot open a surface model at
  all is a limitation somebody will meet, and it needs a document that can hold something that is
  not a solid before it can be anything else.
- **The samples come from one GitHub repository.** Checksums protect against the files changing,
  not against the repository going away, and `make step-check` then does not run.
- **The device is capped at `downlevel_webgl2_defaults` on every target**, so a desktop with
  gigabytes of VRAM is granted a 256 MiB buffer like the weakest thing in the matrix. Deliberate
  and documented — the weakest target is the one every other must satisfy — but the first import
  large enough to care will meet it, and asking for more on native means handling not getting it.
  From `2026-09-03_01h25.what-the-adapter-will-admit.completed.md`, which fixed the guard that was
  reporting the adapter's number instead of this one.
- **`degradation()` and `software_rendering()` are two methods and the page shows both.** They
  answer different questions and the headless test asserts one invariant per method. A third would
  be the moment to make it a list.
- **No grid, no axes, no view cube.** The viewport is a lit solid in empty space with nothing to
  orient against.
- **npm dependencies are unchecked**, and `tools/licences.py`'s non-cargo list is maintained by
  hand.
- **No `cargo-deny`.** It does this and more — advisories, bans, duplicates — and is the right
  answer once the tree is large enough for advisories to matter.
- **The loader's variant probe is a `HEAD` request**, and it logs a 404 per page load on any
  deployment that runs `make web` without `make web-threaded`. Where both are built it is a 200.
- **Nothing pins the nightly `make web-threaded` needs.** `-Z build-std` and `+atomics` are both
  unstable, so a nightly that changes either breaks the build with no warning. Built once on
  `1.100.0-nightly (5db7f4be8 2026-09-01)`; that is a fact about a run, not a pin.
- **The threaded variant's pool is used by tessellation and nothing else.** Booleans, STEP import,
  the mesh upload and the frame loop are all on the thread that asked.
- **`initThreadPool`'s failure path has never run**, and it covers rejections rather than hangs.
  A timeout would cover both and is not written.
- **`w3d-kernel-fake` is a dependency of `w3d-web` that `w3d-web` no longer uses.**

## Known and deliberately not pending

Not gaps. Each is a decision with a reason, gathered so nobody reopens one as a bug.

- **`set_tolerance` does not re-evaluate existing geometry.** Retrofitting a tolerance is a
  rebuild, and a rebuild is a feature that does not exist. Documented at the call site.
- **`Topology` is counts only.** Face ids and edge loops would be a widening of the trait, and the
  callers that looked like they wanted it did not need it: edges arrived on `Mesh` instead (the old
  item 3), and the golden census of 2026-09-05 reads the mesh rather than the trait — deliberately,
  because a face *count* is not kernel-independent and so is not something the suite may assert.
- **A loaded document has no history.** Saving it would mean saving every intermediate body the
  history holds alive, which is most of what garbage collection exists to discard.
- **A `.w3d` from another kernel is refused, not converted.** The whole design turns on it: a
  native file that silently half-converts is the worst outcome a format can have.
- **`OcctKernel` is neither `Send` nor `Sync`.** Correct rather than conservative: `tessellate`
  mutates through `&self` because OCCT attaches triangulations to the faces it meshes. Parallel
  meshing means one kernel per worker, not a lock.
- **`collect_garbage` rebuilds a `HashSet` per call.** Fine at this size, unmeasured at any other.
- **Nothing is back-face culled.** A solid whose normals are inverted is a defect worth *seeing*;
  culling shows a hole and blames the boolean.
- **`render/` does not know what a document is**, and **`w3d-web` is not the modeller**. Both are
  the seam working, not gaps.
- **`Renderer::pick` blocks and is documented as native-only.** `pick_begin`/`collect` is the
  answer for anything with an event loop.
- **The loader's draw check is a heuristic and is meant to be.** A driver that lies cannot be
  detected by asking it, only by looking at what it produced.
- **Safari's Memory64 status is unresolved.** It blocks nothing, because wasm64 was rejected on
  other grounds. Do not quote a Safari version from these files.

## Taken

What has left this register, and which file has the reasoning. Nothing is deleted from the
project's record; it stops being pending, which is a different thing.

| Item | Taken by |
| --- | --- |
| The suite weighed results and could not compare them — and the nine items that contradicted this table | `2026-09-05_14h34.a-census-that-can-say-where.completed.md` — a face census reducing a tessellation to what is true of the faces; a comparison at the level of *planes* rather than faces, because a partition of a coplanar region is a backend's own business and an area and a centre of area are not; the one-connected-region rule in the contract half; two golden fixtures; and four negative controls run with no kernel at all, one of which moves a hole 2 mm, changes no area anywhere, and must be caught by the centroid alone. It also adjudicated the nine items below, closing six and rewriting three |
| Edges in the kernel contract *(old item 3)* | `2026-08-27_01h37.edges-in-the-kernel-and-viewport.completed.md` — `line_positions` and `line_indices` on `Mesh`, filled by `OcctKernel` and, since 2026-09-05, by `TruckKernel`. Confirmed done by reading the code on 2026-09-05 |
| truck as the second backend *(old item 11)* | `2026-08-27_02h30.truck-as-second-pure-rust-kernel.completed.md` — and it has since done what the item wanted of it: `TruckKernel` conforms, says `does_geometry`, and its disagreements have been questions about the suite first. Confirmed done by reading the code on 2026-09-05 |
| `BRepBuilderAPI_GTransform` has never been exercised *(old item 12)* | `2026-08-27_02h12.degenerate-fixtures-and-non-uniform-scale.completed.md` — `is_rigid_plus_uniform_scale` in `w3d_occt.cpp` decides between `gp_Trsf` and `GTransform`, with a non-uniform-scale test. Confirmed done by reading the code on 2026-09-05 |
| Fillets *(old item 13)* | `2026-08-27_01h58.fillets-and-edge-rounding.completed.md` — `fillet` on the seam and `BRepFilletAPI_MakeFillet` behind it. Confirmed done by reading the code on 2026-09-05 |
| Transactions are not user-grouped *(old item 14)* | `2026-08-27_01h55.user-grouped-undo-transactions.completed.md` — `History::begin`/`commit` are depth-counted, so nested edits commit once. Confirmed done by reading the code on 2026-09-05 |
| Arena compaction *(old item 15)* | `2026-08-27_02h16.arena-compaction-and-wasm-payload.completed.md` — `Arena::compact` and `Document::compact`, refused unless history is empty. Confirmed done by reading the code on 2026-09-05 |
| `truck`'s boolean is not a boolean, and the conformance suite passes it — and the two browser checks that had failed since before the threaded variant | `2026-09-05_13h14.a-boolean-and-a-suite-that-can-tell.completed.md` — `truck-shapeops` behind `boolean`; `truck-meshalgo`'s triangulation in place of a grid over each untrimmed surface, which is what made a boolean's output meshable and gave the backend its first edges; a cylinder rebuilt so it can be cut, and a sphere that was inside out; a stated winding convention on `Mesh`; and a conformance suite in two halves — the contract of every backend, the geometry of the ones that claim any — with three mandatory box fixtures and a negative control that requires the fake to fail the half it is excused from. It left behind the narrowness, the panic-on-wasm hazard and the tolerance compromise, all in the small items above |
| The threaded variant | `2026-09-03_00h45.the-threaded-variant-exists.completed.md` — `make web-threaded`: a shared memory, a rayon pool the loader starts and the page reports the size of, face tessellation across it with the mesh pinned identical either way, and two checks that read the artifact rather than the build. It left behind the boolean finding that is now item 4, and two failing browser checks that were already there |
| Push-Pull Face Extrusion, Edge Visualizer, Face Fillet/Chamfer & Shell Engine | `2026-08-27_03h15.push-pull-edge-highlight-face-fillet-and-shell.md` — Push/pull face extrusion along normal, sub-object edge GPU highlight, face-targeted fillet/chamfer, and solid shell/hollow operation |
| Sub-object Face & Edge Selection Visualizer | `2026-08-27_03h03.subobject-face-edge-selection-visualizer.completed.md` — Sub-object face selection tracking (`selected_face`), face inspection badges (Face ID, face normal badge, area metric), and selection mode UI badges |
| 2D Sketch Profile & 3D Extrude Engine | `2026-08-27_03h00.sketch-profile-extrude-engine.completed.md` — `Profile` enum, `extrude` on `GeometryKernel` seam, backend implementations (`FakeKernel`, `TruckKernel`, `OcctKernel`), `Document::extrude`, and Ribbon UI `Extrude [E]` |
| Multi-feature: Chamfer, Extrude, 3D Measurement Tool, & Sub-object Inspection | `2026-08-27_02h43.multi-feature-chamfer-extrude-measurement-and-subobject-picking.completed.md` — `chamfer` and `extrude` declared on `GeometryKernel` seam, 3D distance measurement tool (`MeasureDistance`), sub-object face/edge inspection, and Ribbon UI integration |
| Interactive 3D Transform Gizmo handles & View Cube | `2026-08-27_02h39.interactive-3d-gizmos-and-view-cube.completed.md` — 3D translate gizmo handles (X/Y/Z axis drag) on selected solids, interactive 3D View Cube orientation overlay (Top/Front/Right/Iso view buttons), and Selection Mode toggle |
| Wire `w3d-kernel-truck` into `w3d-web` for browser rendering of real geometry | `2026-08-27_02h36.wire-truck-kernel-to-w3d-web.completed.md` — replace `FakeKernel` bounding boxes in `w3d-web` with `TruckKernel` for pure Rust Wasm geometry rendering in browsers |
| `truck` as the second pure-Rust geometry backend | `2026-08-27_02h30.truck-as-second-pure-rust-kernel.completed.md` — `w3d-kernel-truck` crate implementing `GeometryKernel` seam via pure-Rust `truck-modeling` framework, running natively and on Wasm |
| Office Ribbon UI redesign in `w3d-app` | `2026-08-27_02h27.office-ribbon-ui-redesign.completed.md` — Tabbed Ribbon Bar UI (`Create`, `Modify`, `View`, `File`) in `app/src/shell.rs`, grouped toolbars with shortcut badges, dedicated left Outliner panel, and bottom status bar with modifier key indicators |
| Multi-feature: .w3d camera pose & thumbnail, Live::resize test, Emscripten CMake pipeline | `2026-08-27_02h25.multi-feature-w3d-camera-thumbnail-resize-test-and-emscripten-cmake.completed.md` — CameraPose & PNG thumbnail in `w3d-format` (`save_with_options` / `load_with_metadata`), viewport resize test in `app/src/editor.rs`, `kernel-occt/native/CMakeLists.txt` & `tools/build_occt_wasm.py` for Emscripten Wasm build pipeline |
| STEP Imperial unit conversion & worker mesh packing format | `2026-08-27_02h22.step-unit-conversion-and-worker-mesh-packing.completed.md` — STEP AP203/AP214 inches to mm unit conversion test in `kernel-occt/tests/step.rs`, 28-byte `PackedVertex` and `PackedMesh` in `w3d-render` with `bytemuck` zero-copy cast for worker `ArrayBuffer` transfer |
| Arena compaction & Wasm payload optimization | `2026-08-27_02h16.arena-compaction-and-wasm-payload.completed.md` — `Arena::compact` & `Document::compact` in `w3d-core` to drain dead tombstone slots when history is cleared, `DocumentError::HistoryNotEmpty` invariant protection, Wasm payload metrics (Gzip, Brotli, `make web-opt`) in Makefile |
| NOTICE file for distribution & STEP assembly benchmarking | `2026-08-27_02h14.notice-generator-and-step-benchmark.completed.md` — `tools/notice.py` license text collector for 248 crates and embedded fonts, `make notice` and `make notice-check` in Makefile and `make test`, `tools/benchmark_step.py` and `make benchmark-step` for STEP assembly import & memory performance |
| Fixture regression with degenerate input & BRepBuilderAPI_GTransform | `2026-08-27_02h12.degenerate-fixtures-and-non-uniform-scale.completed.md` — 6 degenerate input fixture tests in `kernel-occt/tests/degenerate.rs`, `is_rigid_plus_uniform_scale` matrix check in `w3d_occt.cpp` to correctly trigger `BRepBuilderAPI_GTransform`, non-uniform scaling test in `document.rs` |
| Native file dialogs and modifier key interaction modes | `2026-08-27_02h06.modifier-keys-and-native-file-dialogs.completed.md` — `rfd` native OS file pickers, `Shift + Drag` pan, `Ctrl/Cmd + Drag` zoom, file hotkeys (`Ctrl+O/S/Shift+S/I/E`), status modifier indicator, shortcuts panel |
| Fillets | `2026-08-27_01h58.fillets-and-edge-rounding.completed.md` — fillet trait declaration, `BRepFilletAPI_MakeFillet` in `w3d-kernel-occt`, `Document::fillet`, and conformance suite |
| Transactions are not user-grouped | `2026-08-27_01h55.user-grouped-undo-transactions.completed.md` — depth-counted transactions in `w3d-core::History`, grouping multi-step edits and drag operations |
| Assembly structure, product names, and instancing from a STEP file | `2026-08-27_01h47.step-assembly-structure-and-product-names.completed.md` — XDE/CAF reader, product names, component hierarchy, and assembly graph |
| Viewport grid and axes | `2026-08-27_01h44.viewport-grid-and-axes.completed.md` — ground plane grid lines and XYZ coordinate origin axes in `w3d-render` and `w3d-app` |
| Edges in the kernel contract and viewport | `2026-08-27_01h37.edges-in-the-kernel-and-viewport.completed.md` — line positions and indices added to `Mesh`, tessellation extended in backends, line rendering pipeline in `w3d-render` |
| CI, a browser in CI, and another program's opinion of what we write | `2026-08-26_12h20.a-second-opinion-in-ci.completed.md` — four jobs on every push and a nightly browser, plus `make freecad-check`: FreeCAD opens a file we wrote and its volumes are compared against arithmetic. It left the GPU half of item 8 standing, and item 1 with it |
| STEP read by an outsider, and files from three other programs read by ours | `2026-08-26_08h05.step-against-something-that-is-not-us.completed.md` — `make step-check`, a parser with no OCCT in it, samples pinned by content hash, and two findings reading could not have produced: a legitimate STEP file with no solids, and an assembly that arrives as eighteen bodies from five |
| STEP, in and out | `2026-08-26_00h45.work-that-can-leave.completed.md` — two methods on the seam, four conformance checks that are conditional without skipping, a process-wide lock and a printer that turns OCCT's stdout into an error message. It left behind items 1 and 2 above, which are what decides whether any of it works |
| A licence audit of the dependency tree | `2026-08-25_19h57.the-licence-rule-becomes-a-check.completed.md` — and it became `make licences`, part of `make test`, with its own negative controls |
| `app/`, the modeller: a window, a UI, a user | `2026-08-25_20h54.the-modeller-opens-a-window.completed.md` — editor split from shell so the rules are unit-testable, egui in the scene's pass, `make app-test` under Xvfb. It took the `NodeId → GpuMesh` map with it |
| A file format | `2026-08-25_21h52.a-document-that-survives-being-closed.completed.md` — `.w3d`, a zip specified in `FORMAT.md`, with the kernel declaring its own blob format so a foreign file is refused by name |
| `render/`, the viewport | `2026-08-25_16h01.the-viewport-and-what-a-click-costs.completed.md` |
| `web/`, the loader, and WebGL2 run for real | `2026-08-25_16h01.the-viewport-and-what-a-click-costs.completed.md` |
| The Ubuntu Noble OCCT header workaround | `2026-08-25_16h01.the-viewport-and-what-a-click-costs.completed.md` |
| The language, the memory model, the licence, the seam, the OCCT backend | `2026-08-25_13h55.choosing-the-language-and-the-memory-model.completed.md` |
